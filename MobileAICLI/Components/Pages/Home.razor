@page "/"
@using Microsoft.AspNetCore.Authorization
@inject NavigationManager NavigationManager
@attribute [Authorize]

<PageTitle>MobileAICLI</PageTitle>

@code {
    protected override void OnInitialized()
    {
        // Redirect to Copilot page for quick access
        NavigationManager.NavigateTo("/copilot", forceLoad: false);
    }
}
