@page "/"
@using Microsoft.AspNetCore.Authorization
@inject NavigationManager NavigationManager
@attribute [Authorize]

<PageTitle>MobileAICLI</PageTitle>

@code {
    protected override async Task OnInitializedAsync()
    {
        // Redirect to Copilot page for quick access
        await Task.CompletedTask;
        NavigationManager.NavigateTo("/copilot", forceLoad: false, replace: true);
    }
}
