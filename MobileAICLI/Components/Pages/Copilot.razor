@page "/copilot"
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.SignalR.Client
@using Microsoft.AspNetCore.Http
@using MobileAICLI.Services
@using MobileAICLI.Models
@using MobileAICLI.Components.Layout
@inject NavigationManager NavigationManager
@inject LocalStorageService LocalStorage
@inject IHttpContextAccessor HttpContextAccessor
@implements IAsyncDisposable
@attribute [Authorize]
@rendermode InteractiveServer

<PageTitle>GitHub Copilot</PageTitle>

<div class="copilot-container d-flex flex-column">
    @* Copilot Status Bar integrated into page *@
    <div class="status-bar border-bottom bg-light">
        <div class="copilot-status-bar">
            <div class="copilot-status-info">
                <span class="status-indicator @(isConnected ? "bg-success" : "bg-danger")"></span>
                <small class="text-muted">
                    @if (isConnected)
                    {
                        <span>Connected</span>
                        @if (!string.IsNullOrEmpty(copilotUser))
                        {
                            <span> • @copilotUser</span>
                        }
                        @if (!string.IsNullOrEmpty(currentModel))
                        {
                            <span> • Model: @currentModel</span>
                        }
                    }
                    else
                    {
                        <span>Disconnected</span>
                    }
                </small>
            </div>
            <div class="copilot-status-actions">
                <button class="btn btn-xs btn-outline-secondary" @onclick="OpenSettings" title="Settings">
                    ⚙️
                </button>
                <select class="form-select form-select-xs" style="width: auto; min-width: 90px;" 
                        @bind="selectedModel" 
                        disabled="@(!isConnected || isLoading)">
                    @foreach (var model in allowedModels)
                    {
                        <option value="@model">@model</option>
                    }
                </select>
                <button class="btn btn-xs btn-outline-secondary" @onclick="CheckCopilotStatus" disabled="@(!isConnected)" title="Refresh">
                    🔄
                </button>
                <button class="btn btn-xs btn-outline-danger" @onclick="ClearHistory" disabled="@(messages.Count == 0)" title="Clear" aria-label="Clear chat history">
                    🗑️
                </button>
            </div>
        </div>
        @if (!string.IsNullOrEmpty(statusError))
        {
            <div class="mt-2">
                <small class="text-danger">@statusError</small>
            </div>
        }
    </div>

    @* 채팅 메시지 영역 *@
    <div class="chat-messages flex-grow-1 overflow-auto px-3 pb-2" @ref="chatContainer">
        @if (messages.Count == 0)
        {
            <div class="text-center text-muted py-5">
                <h5>GitHub Copilot CLI</h5>
                <p>Ask Copilot anything about code, commands, or your project.</p>
                <p><small>Example: "How do I list all files modified in the last week?"</small></p>
            </div>
        }
        else
        {
            @foreach (var message in messages)
            {
                <div class="message mb-3 @(message.IsUser ? "message-user" : "message-copilot")">
                    <div class="message-header mb-1">
                        <small class="text-muted">
                            @(message.IsUser ? "You" : "Copilot") • @message.Timestamp.ToString("HH:mm")
                        </small>
                    </div>
                    <div class="message-content p-3 rounded @(message.IsUser ? "bg-primary text-white" : "bg-light")">
                        @if (message.IsStreaming)
                        {
                            <span>@message.Content</span>
                            <span class="typing-indicator">▋</span>
                        }
                        else
                        {
                            <pre class="mb-0 @(message.IsUser ? "text-white" : "")" style="white-space: pre-wrap; word-wrap: break-word;">@message.Content</pre>
                        }
                        @if (message.HasError)
                        {
                            <div class="mt-2 text-danger">
                                <small>@message.ErrorMessage</small>
                            </div>
                        }
                    </div>
                </div>
            }
        }
    </div>

    @* 입력 영역 *@
    <div class="chat-input border-top px-2 py-2">
        <div class="input-group gap-2">
            <textarea class="form-control" 
                      rows="2" 
                      @bind="currentPrompt" 
                      @bind:event="oninput"
                      @onkeydown="HandleKeyDown"
                      placeholder="Ask Copilot..."
                      disabled="@(isLoading || !isConnected)"></textarea>
            <button class="btn btn-primary btn-send" 
                    @onclick="SendPrompt" 
                    disabled="@(isLoading || !isConnected || string.IsNullOrWhiteSpace(currentPrompt))">
                @if (isLoading)
                {
                    <span class="spinner-border spinner-border-sm" role="status"></span>
                }
                else
                {
                    <span aria-label="Send">✈️</span>
                }
            </button>
        </div>
    </div>
</div>

@* 설정 패널 *@
<CopilotSettingsPanel Settings="copilotSettings" 
                      IsVisible="showSettingsPanel"
                      OnSave="HandleSettingsSave"
                      OnCancel="HandleSettingsCancel" />

<style>
    .copilot-container {
        height: 100vh;
        overflow: hidden;
        padding: 0;
    }

    .status-indicator {
        width: 8px;
        height: 8px;
        border-radius: 50%;
        display: inline-block;
    }
    
    .message-user .message-content {
        margin-left: 20%;
    }
    
    .message-copilot .message-content {
        margin-right: 20%;
    }
    
    .typing-indicator {
        animation: blink 1s infinite;
    }
    
    @@keyframes blink {
        0%, 50% { opacity: 1; }
        51%, 100% { opacity: 0; }
    }
    
    .chat-messages {
        scroll-behavior: smooth;
    }
    
    pre {
        font-family: 'Consolas', 'Monaco', 'Courier New', monospace;
        font-size: 0.9em;
    }

    .status-bar {
        padding: 6px 10px 6px 60px; /* Left padding for sidebar toggle button */
    }

    .copilot-status-bar {
        gap: 6px;
        flex-wrap: wrap;
    }

    .copilot-status-info small {
        font-size: 0.95rem;
    }

    .copilot-status-actions {
        gap: 6px;
        display: flex;
        align-items: center;
    }

    .btn-xs {
        --bs-btn-padding-y: 4px;
        --bs-btn-padding-x: 8px;
        --bs-btn-font-size: 0.9rem;
        min-height: 32px;
    }

    .form-select-xs {
        padding: 4px 28px 4px 8px;
        font-size: 0.95rem;
        min-height: 32px;
    }

    .chat-input .form-control {
        min-height: 52px;
        line-height: 1.35;
    }

    .btn-send {
        min-width: 52px;
        min-height: 52px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0 12px;
        font-size: 1.1rem;
    }

    /* Mobile overrides */
    @@media (max-width: 1080px), (pointer: coarse) {
        /* Fix Title Bar Buttons */
        .copilot-status-actions .btn {
            width: auto !important;
            min-height: 32px !important;
            padding: 4px 8px !important;
            margin-bottom: 0 !important;
        }
        
        .copilot-status-actions .form-select {
            width: auto !important;
            min-height: 32px !important;
            padding-right: 24px !important;
        }

        /* Fix Send Button */
        .chat-input .btn-send {
            width: auto !important;
            min-width: 52px !important;
            margin-bottom: 0 !important;
            flex: 0 0 auto; /* Prevent growing/shrinking */
        }
        
        .chat-input .form-control {
            margin-bottom: 0 !important; /* Remove bottom margin if any */
        }
    }

</style>

@code {
    private HubConnection? hubConnection;
    private List<ChatMessage> messages = new();
    private string currentPrompt = string.Empty;
    private bool isLoading = false;
    private bool isConnected = false;
    private string? copilotUser;
    private string? statusError;
    private ElementReference chatContainer;
    private ChatMessage? currentStreamingMessage;
    private CopilotSettings copilotSettings = new();
    private bool showSettingsPanel = false;
    private string selectedModel = "default";
    private string currentModel = "default";
    private List<string> allowedModels = new() { "default" };
    private bool isMobile = false;

    protected override async Task OnInitializedAsync()
    {
        CheckIsMobile();
        await LoadSettings();
        await InitializeHubConnection();
    }

    private void CheckIsMobile()
    {
        try
        {
            var userAgent = HttpContextAccessor.HttpContext?.Request?.Headers["User-Agent"].ToString() ?? "";
            isMobile = userAgent.Contains("Mobile", StringComparison.OrdinalIgnoreCase) || 
                       userAgent.Contains("Android", StringComparison.OrdinalIgnoreCase) || 
                       userAgent.Contains("iPhone", StringComparison.OrdinalIgnoreCase);
        }
        catch
        {
            // Fallback if context is not available
            isMobile = false; 
        }
    }

    private async Task LoadSettings()
    {
        var saved = await LocalStorage.GetItemAsync<CopilotSettings>("copilot-settings");
        if (saved != null)
        {
            copilotSettings = saved;
        }
        else
        {
            // 기본값: Medium 프리셋
            copilotSettings.ApplyPreset("medium");
        }
    }

    private void OpenSettings()
    {
        showSettingsPanel = true;
    }

    private async Task HandleSettingsSave(CopilotSettings settings)
    {
        copilotSettings = settings;
        await LocalStorage.SetItemAsync("copilot-settings", settings);
        showSettingsPanel = false;
        StateHasChanged();
    }

    private Task HandleSettingsCancel()
    {
        showSettingsPanel = false;
        StateHasChanged();
        return Task.CompletedTask;
    }

    private async Task InitializeHubConnection()
    {
        var cookieHeader = HttpContextAccessor.HttpContext?.Request?.Headers?["Cookie"].ToString();

        hubConnection = new HubConnectionBuilder()
            .WithUrl(NavigationManager.ToAbsoluteUri("/copilothub"), options =>
            {
                if (!string.IsNullOrEmpty(cookieHeader))
                {
                    options.Headers.Add("Cookie", cookieHeader);
                }
            })
            .WithAutomaticReconnect()
            .Build();

        // 출력 수신
        hubConnection.On<string>("ReceiveOutput", async (content) =>
        {
            if (currentStreamingMessage != null)
            {
                currentStreamingMessage.Content += content;
                await InvokeAsync(StateHasChanged);
                await ScrollToBottom();
            }
        });

        // 에러 수신
        hubConnection.On<string>("ReceiveError", async (content) =>
        {
            if (currentStreamingMessage != null)
            {
                currentStreamingMessage.Content += content;
                currentStreamingMessage.HasError = true;
                await InvokeAsync(StateHasChanged);
                await ScrollToBottom();
            }
        });

        // 완료 수신
        hubConnection.On<bool, string?>("ReceiveComplete", async (success, error) =>
        {
            if (currentStreamingMessage != null)
            {
                currentStreamingMessage.IsStreaming = false;
                if (!success && !string.IsNullOrEmpty(error))
                {
                    currentStreamingMessage.HasError = true;
                    currentStreamingMessage.ErrorMessage = error;
                }
                currentStreamingMessage = null;
            }
            isLoading = false;
            await InvokeAsync(StateHasChanged);
        });

        hubConnection.Reconnecting += error =>
        {
            isConnected = false;
            InvokeAsync(StateHasChanged);
            return Task.CompletedTask;
        };

        hubConnection.Reconnected += connectionId =>
        {
            isConnected = true;
            InvokeAsync(StateHasChanged);
            return Task.CompletedTask;
        };

        hubConnection.Closed += error =>
        {
            isConnected = false;
            InvokeAsync(StateHasChanged);
            return Task.CompletedTask;
        };

        try
        {
            await hubConnection.StartAsync();
            isConnected = true;
            await CheckCopilotStatus();
        }
        catch (Exception ex)
        {
            statusError = $"Failed to connect: {ex.Message}";
        }
    }

    private async Task CheckCopilotStatus()
    {
        if (hubConnection is null) return;

        try
        {
            statusError = null;
            var result = await hubConnection.InvokeAsync<CopilotStatusResult>("CheckStatus");
            
            if (result.Installed && result.Authenticated)
            {
                copilotUser = result.User;
                statusError = null;
            }
            else
            {
                statusError = result.Error ?? "Copilot CLI is not properly configured";
                copilotUser = null;
            }

            // Update model information
            currentModel = result.CurrentModel ?? "default";
            if (result.AllowedModels?.Count > 0)
            {
                allowedModels = result.AllowedModels;
                // Set selected model to current model if available
                if (allowedModels.Contains(currentModel))
                {
                    selectedModel = currentModel;
                }
            }

        }
        catch (Exception ex)
        {
            statusError = $"Failed to check status: {ex.Message}";
        }
    }

    private async Task SendPrompt()
    {
        if (string.IsNullOrWhiteSpace(currentPrompt) || hubConnection is null) return;

        var prompt = currentPrompt.Trim();
        currentPrompt = string.Empty;

        // 사용자 메시지 추가
        messages.Add(new ChatMessage
        {
            Content = prompt,
            IsUser = true,
            Timestamp = DateTime.Now
        });

        // Copilot 응답 메시지 준비 (스트리밍)
        currentStreamingMessage = new ChatMessage
        {
            Content = string.Empty,
            IsUser = false,
            IsStreaming = true,
            Timestamp = DateTime.Now
        };
        messages.Add(currentStreamingMessage);

        isLoading = true;
        StateHasChanged();
        await ScrollToBottom();

        try
        {
            // CopilotSettings를 CopilotToolSettings로 변환
            var toolSettings = new CopilotToolSettings 
            { 
                CopilotSettings = copilotSettings 
            };
            // Validate selected model against allowed list
            var modelToUse = allowedModels.Contains(selectedModel) ? selectedModel : currentModel;
            await hubConnection.SendAsync("SendPrompt", prompt, toolSettings, modelToUse);
        }
        catch (Exception ex)
        {
            currentStreamingMessage.Content = $"Error: {ex.Message}";
            currentStreamingMessage.IsStreaming = false;
            currentStreamingMessage.HasError = true;
            currentStreamingMessage = null;
            isLoading = false;
        }
    }

    private async Task HandleKeyDown(KeyboardEventArgs e)
    {
        // Mobile: Enter operates as default (newline), manual send required
        // Desktop: Enter sends (unless Shift is pressed)
        if (!isMobile && e.Key == "Enter" && !e.ShiftKey)
        {
            await SendPrompt();
        }
    }

    private void ClearHistory()
    {
        messages.Clear();
        currentStreamingMessage = null;
    }

    private async Task ScrollToBottom()
    {
        await Task.Delay(50); // DOM 업데이트 대기
        // JavaScript interop으로 스크롤
    }

    public async ValueTask DisposeAsync()
    {
        if (hubConnection is not null)
        {
            await hubConnection.DisposeAsync();
        }
    }

    private class ChatMessage
    {
        public string Content { get; set; } = string.Empty;
        public bool IsUser { get; set; }
        public bool IsStreaming { get; set; }
        public bool HasError { get; set; }
        public string? ErrorMessage { get; set; }
        public DateTime Timestamp { get; set; }
    }

    // Hub에서 반환하는 타입과 일치해야 함
    private class CopilotStatusResult
    {
        public bool Installed { get; set; }
        public string? Version { get; set; }
        public bool Authenticated { get; set; }
        public string? User { get; set; }
        public string? Error { get; set; }
        public string? CurrentModel { get; set; }
        public List<string>? AllowedModels { get; set; }
    }
}
